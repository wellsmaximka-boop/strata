# Rokit installs the right rojo for whatever machine you are on, from
# rokit.toml. A Windows .exe checked in would be dead weight on the Mac.
rojo.exe
rojo
luau.exe

# Build output. Everything in this game is generated by the scripts in src/ —
# terrain, camp, UI — so the place file is regenerated with `rojo build`.
# If you ever edit something directly in Studio and want to keep it, say so and
# this line comes out.
Strata.rbxlx
Strata.rbxlx.lock

*.rbxl
*.rbxlx.bak
.DS_Store
Thumbs.db
